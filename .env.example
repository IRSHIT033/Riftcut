# Copy to .env.local and fill in. Also set these in your hosting provider.

# Mixpanel project token (browser SDK). Public by design — safe to expose to
# the client. Analytics no-op silently when this is unset.
NEXT_PUBLIC_MIXPANEL_TOKEN=
